server:
  port: 8004
spring:
  application:
    name: config-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml #指定为yaml的文件格式

#注意nacos的配置文件中的后缀名为yaml不能为yml